let movieTitle: string  = "Her"
movieTitle = "inception"

let age: number = 32;

let isAlive:boolean = true;

// Type inderence 

let day = "Tuesday"

// wil show error because day is type inferenced to string but later assgined to number
day = 2

let jsonData:any ="hello"


//No error because jsonData is 'any'
jsonData = 42
